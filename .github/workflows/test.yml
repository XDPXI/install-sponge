name: Test Install Sponge

on:
  schedule:
    - cron: "0 */24 * * *"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: xdpxi/install-sponge@v1
        with:
          version: "1.1.3"
      - run: sponge -v
